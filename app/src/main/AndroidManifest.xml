<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baolema">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/elemelogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/elemelogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.ShopActivity" />
        <activity android:name=".ui.home.OrderCommitActivity" />
        <activity android:name=".ui.home.LocationActivity" />
        <activity android:name=".ui.mine.SettingActivity" />
        <activity android:name=".ui.mine.MinePhoneActivity" />
        <activity android:name=".ui.order.OrderInfActivity" />
        <activity android:name=".ui.mine.LoginActivity" />
        <activity android:name=".ui.GuideActivity" />
    </application>

</manifest>